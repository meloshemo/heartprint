/**
 * Web reklam alanı — NATIVE tarafı.
 *
 * Uygulama içinde reklamları AdMob yönetiyor (geçiş + ödüllü), bu yüzden
 * burada gösterilecek bir şey yok. Metro, web build'inde bunun yerine
 * WebAdSlot.web.tsx dosyasını seçer.
 */
export function WebAdSlot(_props: { slot: 'result' }): null {
  return null;
}
